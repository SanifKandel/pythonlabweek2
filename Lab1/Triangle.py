#Write a program that reads the length of the base and the height of a right-angled triangle and print the area . Every number is given on a separate line.

len1 =float(input('Enter the length of base: \n'))
hgt1 =float(input('Enter the height: \n'))
area = 1/2 * (len1 * hgt1)
print (area)